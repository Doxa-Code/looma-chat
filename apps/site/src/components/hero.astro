---
import Button from "./ui/button.astro";
import { ArrowRight, Play } from "@lucide/astro"
---

<section class="relative overflow-hidden bg-gradient-to-b from-background to-muted py-20 lg:py-32">
      <div class="container mx-auto px-4">
        <div class="mx-auto max-w-7xl">
          <div class="grid lg:grid-cols-2 gap-12 items-center">
            {/* Coluna do texto */}
            <div class="text-center lg:text-left">
              <div class="mb-8 inline-flex items-center rounded-full border bg-card px-4 py-2 text-sm">
                <span class="mr-2 h-2 w-2 rounded-full bg-green-500"></span>
                IA Especializada em Odontologia
              </div>

              <h1 class="mb-6 text-4xl font-bold tracking-tight text-balance lg:text-6xl">
                Reative Pacientes Inativos com a <span class="text-primary">Inteligência da Olivia</span>
              </h1>

              <p class="mb-8 text-xl text-muted-foreground text-pretty lg:text-2xl">
                Transforme sua clínica odontológica com tecnologia que gera resultados. Aumente sua receita reativando
                pacientes que não voltam há meses.
              </p>

              <div class="flex flex-col gap-4 sm:flex-row lg:justify-start sm:justify-center">
                <Button size="lg" class="bg-secondary hover:bg-secondary/90 text-lg px-8">
                  Começar Teste Grátis
                  <ArrowRight class="ml-2 h-5 w-5" />
                </Button>
                <Button size="lg" variant="outline" class="text-lg px-8 bg-transparent">
                  <Play class="mr-2 h-5 w-5" />
                  Ver Demonstração
                </Button>
              </div>

              <div class="mt-12 flex items-center justify-center lg:justify-start space-x-8 text-sm text-muted-foreground">
                <div class="flex items-center">
                  <span class="mr-2 h-2 w-2 rounded-full bg-green-500"></span>
                  Sem instalação
                </div>
                <div class="flex items-center">
                  <span class="mr-2 h-2 w-2 rounded-full bg-green-500"></span>
                  Resultados em 7 dias
                </div>
                <div class="flex items-center">
                  <span class="mr-2 h-2 w-2 rounded-full bg-green-500"></span>
                  Suporte especializado
                </div>
              </div>
            </div>

            <div class="relative lg:order-last">
              <div class="relative">
                <img
                  src="/hero-dental-ai.jpg"
                  alt="Dentista utilizando tecnologia IA para reativar pacientes"
                  width={600}
                  height={500}
                  class="rounded-2xl shadow-2xl"
                />
                {/* Overlay com elementos visuais */}
                <div class="absolute -bottom-6 -left-6 bg-white rounded-xl p-4 shadow-lg border">
                  <div class="flex items-center space-x-3">
                    <div class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></div>
                    <span class="text-sm font-medium">Olivia ativa</span>
                  </div>
                  <p class="text-xs text-muted-foreground mt-1">Reativando pacientes...</p>
                </div>

                <div class="absolute -top-6 -right-6 bg-primary text-white rounded-xl p-4 shadow-lg">
                  <div class="text-2xl font-bold">+47%</div>
                  <p class="text-xs opacity-90">Retorno de pacientes</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Background decoration */}
      <div class="absolute inset-0 -z-10 overflow-hidden">
        <div class="absolute left-1/2 top-1/2 h-96 w-96 -translate-x-1/2 -translate-y-1/2 rounded-full bg-primary/5 blur-3xl"></div>
      </div>
    </section>